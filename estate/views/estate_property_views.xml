<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="estate_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'search_default_available': True}</field>
    </record>

   <record id="estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties" decoration-success="status == 'offer_received' or status == 'offer_accepted'" decoration-muted="status == 'sold'" decoration-bf="status == 'offer_accepted'">
                <field name="name" string="Title" width="250" class="text-left"/>
                <field name="postcode" string="Postcode" width="120" class="text-left"/>
                <field name="bedrooms" string="Bedrooms" width="100" class="text-left"/>
                <field name="living_area" string="Living Area (sqm)" width="160" class="text-left"/>
                <field name="expected_price" string="Expected Price" width="160" class="text-left"/>
                <field name="selling_price" string="Selling Price" width="150" class="text-left"/>
                <field name="date_availability" string="Available From" width="140" class="text-left" optional="True"/>
                <field name="property_type_id" string="Property Type" width="140" class="text-left"/>
            </list>
        </field>
    </record>

    <record id="estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <sheet>
                <group>
                <group class="col-md-6">
                <button name="set_sold" string="SOLD" type="object" class="oe_highlight"/>
                <button name="set_canceled" string="CANCELLED" type="object" class="oe_highlight"/>
                </group>
                <group class="col-md-6">
                <field name="status" widget="statusbar"/>
                </group>
                </group>
                    <group>
                        <h1><field name="name" string="Title"/></h1>
                    </group>
                    <field name="tags_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    <group>
                    <group class="col-md-6">
                        <field name="property_type_id" string="Property Type" widget="selection"/>
                        <field name="postcode" string="Postcode"/>
                        <field name="date_availability" string="Available From"/>
                    </group>
                    <group class="col-md-6">
                        <field name="expected_price" string="Expected Price"/>
                        <field name="best_offer" string="Best Offer"/>
                        <field name="selling_price" string="Selling Price"/>
                    </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" string="Description"/>
                                <field name="bedrooms" string="Bedrooms"/>
                                <field name="living_area" string="Living Area (sqm)"/>
                                <field name="facades" string="Facades"/>
                                <field name="garage" string="garage"/>
                                <field name="garden" string="Garden"/>
                                <field name="garden_area" string="Garden Area (sqm)" invisible="not garden"/>
                                <field name="garden_orientation" string="Garden Orientation" invisible="not garden"/>
                                <field name="total_area" string="Total Area (sqm)"/>
                            </group>
                        </page>
                        <page string="Offers">
                                <field name="offer_ids" readonly="status not in ('new', 'offer_received')">
                                    <list string="Offers" editable="bottom" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                                        <field name="price" string="Price"/>
                                        <field name="partner_id" string="Buyer"/>
                                        <field name="date_deadline" string="Deadline"/>
                                        <button name="accept_offer" icon="fa-check" type="object" class="oe_highlight" invisible="status"/>
                                        <button name="refuse_offer" icon="fa-close" type="object" class="oe_highlight" invisible="status"/>
                                        <field name="validity" string="Validity (days)"/>
                                    </list>
                                </field>
                        </page>
                        <page string="Other info">
                            <group>
                                <field name="salesperson_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

       <record id="estate_property_search" model="ir.ui.view">
            <field name="name">estate.property.search</field>
            <field name="model">estate.property</field>
            <field name="arch" type="xml">
                <search string="Estate Property Search">
                  <field name="name" string="Title"/>
                  <field name="postcode" string="Postcode"/>
                  <field name="expected_price" string="Expected Price"/>
                  <field name="bedrooms" string="Bedrooms"/>
                  <field name="living_area" string="Living Area (sqm)"
                  filter_domain="[('living_area', '>=', self)]"/>
                  <field name="facades" string="Facades"/>
                  <field name="property_type_id" string="Property Type"/>
                  <filter string="Available" name="available" domain="['|',('status', '=', 'new'),('status', '=', 'offer_received')]"/>
                  <filter string="Postcode" name="postcode" context="{'group_by':'postcode', 'residual_visible':True}"/>
                </search>
            </field>
        </record>
    <record id="estate_property_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_example" default_group_by="property_type_id" records_draggable="false">
                <field name="status"/>
                <field name="expected_price"/>
                <field name="best_offer"/>
                <field name="selling_price"/>
                <field name="tags_ids"/>
                <templates>
                    <t t-name="card">
                        <div>
                            <h5><field name="name"/></h5>
                            Expected Price: <field name="expected_price"/>
                            <div t-if="record.status.raw_value == 'offer_received'">
                            Best Offer: <field name="best_offer"/>
                            </div>
                            <div t-if="record.status.raw_value == 'offer_accepted'">
                                Selling Price: <field name="selling_price"/>
                            </div>
                            <field name="tags_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
        </record>
</odoo>